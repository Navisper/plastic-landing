<section class="relative overflow-hidden">
    <div
            class="absolute inset-0 bg-gradient-to-br from-primary-50/20 to-emerald-50/10 dark:from-gray-900 dark:to-gray-800"
    >
    </div>

    <div class="container mx-auto px-4 py-16 md:py-20 relative z-10">
        <div class="max-w-6xl mx-auto text-center">
            <!-- Título optimizado para 2 líneas exactas -->
            <h1
                    class="text-4xl md:text-5xl lg:text-6xl xl:text-7xl font-bold mb-8 uppercase leading-tight tracking-tight"
            >
                <!-- Línea 1: Ocupa todo el ancho -->
                <span class="block w-full whitespace-nowrap">
                    ENCUENTRA RECICLAJE DISPONIBLE
                </span>
                <!-- Línea 2: Con gradiente y completa -->
                <span class="block w-full bg-gradient-to-r from-primary-600 to-emerald-500 bg-clip-text text-transparent whitespace-nowrap">
                    EN TIEMPO REAL Y EN TU CIUDAD
                </span>


            </h1>

            <!-- Subtítulo compacto -->
            <p
                    class="text-lg md:text-xl text-gray-600 dark:text-gray-300 mb-10 max-w-2xl mx-auto leading-relaxed"
            >
                Somos una plataforma que te muestra puntos de reciclaje
                cercanos, cantidades disponibles y materiales específicos listos
                para recoger.
            </p>

            <!-- CTA principal -->
            <div class="flex flex-col sm:flex-row gap-4 justify-center mb-12">
                <a
                        href="#mapa-ibague"
                        class="bg-gradient-to-r from-primary-500 to-emerald-600 text-white px-8 md:px-10 py-4 rounded-full text-lg font-semibold hover:shadow-xl hover:shadow-primary-500/30 transition-all duration-300 transform hover:-translate-y-1 shadow-lg"
                >
                    Quiero ver dónde hay reciclaje disponible, ya!
                </a>
            </div>

            <!-- Stats optimizados -->
            <div
                    class="grid grid-cols-1 md:grid-cols-2 gap-8 max-w-2xl mx-auto"
            >
                <div class="text-center p-5 bg-white/60 dark:bg-gray-800/60 rounded-xl backdrop-blur-sm">
                    <div
                            class="text-4xl font-bold text-primary-600 dark:text-primary-400 mb-2"
                    >
                        -70%
                    </div>
                    <div class="text-base text-gray-600 dark:text-gray-400 font-medium">
                        Menos tiempo perdido
                    </div>
                </div>

                <div class="text-center p-5 bg-white/60 dark:bg-gray-800/60 rounded-xl backdrop-blur-sm">
                    <div
                            class="text-4xl font-bold text-primary-600 dark:text-primary-400 mb-2"
                    >
                        +200%
                    </div>
                    <div class="text-base text-gray-600 dark:text-gray-400 font-medium">
                        Más toneladas procesadas
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>