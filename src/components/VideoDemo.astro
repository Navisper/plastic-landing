---
// Reemplaza este ID con el ID de tu video de YouTube
const youtubeId = "TU_ID_DE_YOUTUBE_AQUI";
---

<section id="video-demo" class="py-20 bg-gray-50 dark:bg-gray-800">
    <div class="container mx-auto px-4">
    <h2 class="text-4xl font-bold text-center mb-4">Demo en video</h2>
    <p class="text-gray-600 dark:text-gray-400 text-center mb-12 max-w-2xl mx-auto">
        Mira cómo Plastic transforma el proceso de reciclaje con tecnología innovadora
    </p>
    
    <div class="max-w-4xl mx-auto">
        <!-- Contenedor del video -->
        <div class="relative aspect-video bg-black rounded-2xl overflow-hidden shadow-2xl">
        {youtubeId ? (
            <iframe
            src={`https://www.youtube.com/embed/${youtubeId}?rel=0&modestbranding=1`}
            class="absolute inset-0 w-full h-full"
            frameborder="0"
            allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture"
            allowfullscreen
            title="Demo de Plastic App"
            loading="lazy"
            />
        ) : (
            <!-- Placeholder si no hay ID de YouTube -->
            <div class="absolute inset-0 flex flex-col items-center justify-center bg-gradient-to-br from-gray-900 to-gray-800">
            <div class="text-center p-8">
                <div class="text-6xl mb-4">📱</div>
                <h3 class="text-2xl font-bold text-white mb-2">Demo de Plastic App</h3>
                <p class="text-gray-400 mb-6">
                Inserta el ID de tu video de YouTube en el componente
                </p>
                <div class="inline-flex items-center gap-2 text-sm text-gray-300 bg-gray-800/50 px-4 py-2 rounded-lg">
                <span>ID de ejemplo:</span>
                <code class="bg-gray-900 px-2 py-1 rounded">dQw4w9WgXcQ</code>
                </div>
            </div>
            </div>
        )}
        </div>
        
        <!-- Instrucciones para insertar video -->
        {!youtubeId && (
        <div class="mt-8 p-6 bg-primary-50 dark:bg-primary-900/20 rounded-xl border border-primary-200 dark:border-primary-800">
            <h4 class="font-semibold text-primary-700 dark:text-primary-400 mb-2">¿Cómo agregar tu video?</h4>
            <ol class="text-sm text-gray-600 dark:text-gray-400 space-y-1 list-decimal list-inside">
            <li>Sube tu video demo a YouTube</li>
            <li>Copia el ID del video (ej: en https://youtu.be/dQw4w9WgXcQ, el ID es <code>dQw4w9WgXcQ</code>)</li>
            <li>Reemplaza <code>"TU_ID_DE_YOUTUBE_AQUI"</code> en el archivo <code>VideoDemo.astro</code></li>
            </ol>
        </div>
        )}
    </div>
    </div>
</section>